#include<stdio.h>
int main()
{
int base,exponent,k,power=1;
scanf("%d\n",&base);
scanf("%d\n",exponent);
for(i=1;i<exponent;i++)
{
power = power*base
}
printf("power of number %d is %d",base,power);
return 0;
}
